# Publishes a GitHub Release whenever package.json's version on main has no
# release yet. Idempotent: every push to main checks the current version and
# creates a release only if one doesn't already exist, so merging a version bump
# (or this workflow itself) publishes the release automatically.
# After creating the release, builds native desktop installers (macOS/Windows/Linux)
# and attaches them to the release alongside the auto-generated source archives.
name: Release on version change

on:
  push:
    branches: ["main"]
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.ver.outputs.version }}
      created: ${{ steps.check.outputs.exists == 'false' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read version from package.json
        id: ver
        run: echo "version=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Check whether this release already exists
        id: check
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "v${{ steps.ver.outputs.version }}" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Release v${{ steps.ver.outputs.version }} already exists — nothing to do."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Extract release notes from CHANGELOG.md
        if: steps.check.outputs.exists == 'false'
        run: |
          VERSION="${{ steps.ver.outputs.version }}"
          # Pull the section between "## [VERSION]" and the next "## [" header.
          awk -v ver="$VERSION" '
            index($0, "## [" ver "]") == 1 { flag=1; next }
            flag && index($0, "## [") == 1 { flag=0 }
            flag { print }
          ' CHANGELOG.md > RELEASE_NOTES.md || true
          # Fall back to a stub if the changelog has no matching section.
          if ! grep -q "[^[:space:]]" RELEASE_NOTES.md 2>/dev/null; then
            printf 'Release v%s\n' "$VERSION" > RELEASE_NOTES.md
          fi
          echo "----- release notes -----"
          cat RELEASE_NOTES.md

      - name: Create GitHub release
        if: steps.check.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "v${{ steps.ver.outputs.version }}" \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "v${{ steps.ver.outputs.version }}" \
            --notes-file RELEASE_NOTES.md

      - name: Setup Node.js
        if: steps.check.outputs.exists == 'false'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        if: steps.check.outputs.exists == 'false'
        run: npm ci

      - name: Publish to npm
        if: steps.check.outputs.exists == 'false'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_ACCESS_TOKEN }}
        run: npm publish

  build-desktop:
    name: Build desktop app (${{ matrix.os }})
    needs: release
    if: needs.release.outputs.created == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            artifact_pattern: "app/dist-app/*.dmg"
            platform_flag: "--mac"
          - os: windows-latest
            artifact_pattern: "app/dist-app/*.exe"
            platform_flag: "--win"
          - os: ubuntu-latest
            artifact_pattern: "app/dist-app/*.AppImage"
            platform_flag: "--linux"
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build desktop app
        run: npm run build:app -- ${{ matrix.platform_flag }}

      - name: Upload installer to release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          shopt -s nullglob
          files=(${{ matrix.artifact_pattern }})
          if [ ${#files[@]} -eq 0 ]; then
            echo "No installer artifacts found matching ${{ matrix.artifact_pattern }}"
            exit 1
          fi
          gh release upload "v${{ needs.release.outputs.version }}" \
            "${files[@]}" \
            --repo "$GITHUB_REPOSITORY" \
            --clobber
